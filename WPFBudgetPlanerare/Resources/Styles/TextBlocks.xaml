<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Page Title -->
    <Style x:Key="PageTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource App.Text.Primary}"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>

    <!-- Section Header -->
    <Style x:Key="SectionHeader" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource App.Text.Primary}"/>
        <Setter Property="Margin" Value="0,0,0,20"/>
    </Style>

    <!-- Header Welcome Text -->
    <Style x:Key="WelcomeText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource App.Background}"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="Light"/>
        <Setter Property="Opacity" Value="0.85"/>
    </Style>

    <!-- Header User Name -->
    <Style x:Key="UserNameText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="22"/>
        <Setter Property="Margin" Value="0,10,0,0"/>
    </Style>

    <!-- Header Info Text -->
    <Style x:Key="HeaderInfoText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="Margin" Value="0,8,0,0"/>
    </Style>

    <!-- Category Text in Transaction Card -->
    <Style x:Key="CategoryText" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="17"/>
        <Setter Property="Foreground" Value="{StaticResource App.Text.Primary}"/>
    </Style>

    <!-- Amount Text -->
    <Style x:Key="AmountText" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <!-- Description Text -->
    <Style x:Key="DescriptionText" TargetType="TextBlock">
        <Setter Property="FontStyle" Value="Italic"/>
        <Setter Property="Foreground" Value="{StaticResource App.Text.Muted}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Margin" Value="0,0,0,10"/>
    </Style>

    <!-- Date Label -->
    <Style x:Key="DateLabelText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource App.Text.Muted}"/>
        <Setter Property="FontWeight" Value="Medium"/>
    </Style>

    <!-- Date Value -->
    <Style x:Key="DateValueText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource App.Text.Primary}"/>
    </Style>

    <!-- Badge Text -->
    <Style x:Key="BadgeText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource App.Text.Primary}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <!-- Form Label -->
    <Style x:Key="FormLabel" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource App.Text.Primary}"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <!-- Subtitle Text -->
    <Style x:Key="SubtitleText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource App.Text.Muted}"/>
        <Setter Property="Margin" Value="0,5,0,15"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <!-- Summary Value Large -->
    <Style x:Key="SummaryValueLarge" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <!-- Summary Label -->
    <Style x:Key="SummaryLabel" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

</ResourceDictionary>