<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- TextBox Style -->
    <Style x:Key="FormTextBox" TargetType="TextBox">
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="Margin" Value="0,0,0,15"/>
        <Setter Property="BorderBrush" Value="{StaticResource App.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="{StaticResource App.Surface}"/>
        <Setter Property="Foreground" Value="{StaticResource App.Text.Primary}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="5">
                        <ScrollViewer x:Name="PART_ContentHost" 
                                      Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource App.Accent}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox Style -->
    <Style x:Key="FormComboBox" TargetType="ComboBox">
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="Margin" Value="0,0,0,15"/>
        <Setter Property="BorderBrush" Value="{StaticResource App.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="{StaticResource App.Surface}"/>
        <Setter Property="Foreground" Value="{StaticResource App.Text.Primary}"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <!-- DatePicker Style -->
    <Style x:Key="FormDatePicker" TargetType="DatePicker">
        <Setter Property="Margin" Value="0,0,0,15"/>
        <Setter Property="BorderBrush" Value="{StaticResource App.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Background" Value="{StaticResource App.Surface}"/>
        <Setter Property="Foreground" Value="{StaticResource App.Text.Primary}"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <!-- RadioButton Style -->
    <Style x:Key="FormRadioButton" TargetType="RadioButton">
        <Setter Property="Margin" Value="5,5,15,5"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{StaticResource App.Text.Primary}"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

</ResourceDictionary>