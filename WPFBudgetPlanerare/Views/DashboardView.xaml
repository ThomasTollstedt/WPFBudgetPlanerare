<UserControl x:Class="WPFBudgetPlanerare.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFBudgetPlanerare"
             xmlns:models="clr-namespace:WPFBudgetPlanerare.Models"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource App.Background}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- User Name Header -->
        <Border Grid.Row="0" Style="{StaticResource UserHeaderCard}">
            <StackPanel>
                <TextBlock Text="Välkommen" Style="{StaticResource WelcomeText}"/>
                <TextBlock Style="{StaticResource UserNameText}">
                    <Run Text=""/>
                    <Run Text="{Binding UserName, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock Style="{StaticResource HeaderInfoText}">
                    <Run Text="Årsinkomst: "/>
                    <Run Text="{Binding AnnualIncome, Mode=OneWay, StringFormat='{}{0:N0} kr'}" FontWeight="SemiBold"/>
                </TextBlock>
            </StackPanel>
        </Border>
        
        <!-- Filter Radio Buttons -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Background="Transparent"
                    Margin="15,0,15,10">
            <RadioButton Content="Visa Allt" 
                         Command="{Binding FilterCommand}" 
                         CommandParameter="{x:Static models:TransactionFilterType.All}"
                         Style="{StaticResource FormRadioButton}"/>
            <RadioButton Content="Visa Inkomster" 
                         Command="{Binding FilterCommand}" 
                         CommandParameter="{x:Static models:TransactionFilterType.Income}"
                         Style="{StaticResource FormRadioButton}"/>
            <RadioButton Content="Visa Utgifter" 
                         Command="{Binding FilterCommand}" 
                         CommandParameter="{x:Static models:TransactionFilterType.Expenses}"
                         Style="{StaticResource FormRadioButton}"/>
        </StackPanel>

        <!-- Transactions List -->
        <ListView Grid.Row="2"
                  ItemsSource="{Binding Transactions}"
                  ItemTemplate="{StaticResource TransactionItemTemplate}"
                  Margin="15,0,15,15"
                  HorizontalContentAlignment="Stretch"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  Background="Transparent"
                  BorderThickness="0"/>
    </Grid>
</UserControl>
